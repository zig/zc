class test {
    class lolo {
	int titi;
    }
    
    int a;

    static int operator_plus(int a, lolo l) {
	return a + l.titi;
    }
    
    int toto(int x, float y)
    {
	lolo b;

	b.titi;
	return (a + x / y + a) * x + b.titi + b;
    }
}
